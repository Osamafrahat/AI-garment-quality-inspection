# Edge node (Raspberry Pi 5). On PC, mock mode only needs the base set.
numpy>=1.26
pillow>=10.0
scikit-learn>=1.4
joblib>=1.3
requests>=2.31

# Raspberry Pi extras (install on the Pi):
# picamera2>=0.3
# RPi.GPIO>=0.7
# smbus2>=0.4
# paho-mqtt>=2.0

# Optional ONNX/TFLite runtimes:
# onnxruntime>=1.17
# tflite-runtime>=2.14
